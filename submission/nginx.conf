events {
    worker_connections 1024;
}
http {
    upstream backend {
        least_conn;
        server api1:9999;
        server api2:9999;
    }
    server {
        listen 9999;
        location / {
            proxy_pass http://backend;
            proxy_set_header Host $host;
        }
    }
}
