version: '3.8'

services:
  lb:
    image: nginx:alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: "50MB"

  api1:
    build: .
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "150MB"

  api2:
    build: .
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "150MB"
